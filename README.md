Web application dedicated to cat rescue efforts. Main features include an interactive map and adoption page.
In order to run locally on your device you will need to install Node.js and Python. You will also need a Mapbox GL JS API key in a .env file.
